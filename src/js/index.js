document.addEventListener("DOMContentLoaded", () => {

    //Esperar a que cargue el html

    //Validaciones y select
    formulario();
    agregarEstados();
    agregarEscuelas();
    mostrarConyugue();

    //Menu responsive
    mobileMenu();

    //Eliminar registros
    eliminarRegistro();
 

  });